{
  "name": "wingmate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Big ideas deserve a good wingmate. A guided, repeatable method for building apps with Microsoft Copilot Cowork.",
  "license": "MIT",
  "bin": {
    "wingmate": "./bin/wingmate.mjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "build:site": "node scripts/build-site.mjs",
    "check": "npm test && npm run build && npm run build:site",
    "export": "node bin/wingmate.mjs export"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "fflate": "0.8.3"
  }
}
